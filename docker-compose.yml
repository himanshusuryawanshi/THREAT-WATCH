services:

  postgres:
    image: imresamu/postgis:16-3.4 
    container_name: threatwatch_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: threatwatch
      POSTGRES_PASSWORD: threatwatch_dev
      POSTGRES_DB: threatwatch
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/001_events.sql:/docker-entrypoint-initdb.d/001_events.sql
      - ./db/002_conflicts.sql:/docker-entrypoint-initdb.d/002_conflicts.sql
      - ./db/003_articles.sql:/docker-entrypoint-initdb.d/003_articles.sql
      - ./db/004_humanitarian.sql:/docker-entrypoint-initdb.d/004_humanitarian.sql
      - ./db/005_actors.sql:/docker-entrypoint-initdb.d/005_actors.sql
      - ./db/006_actor_relationships.sql:/docker-entrypoint-initdb.d/006_actor_relationships.sql
      - ./db/007_geo_boundaries.sql:/docker-entrypoint-initdb.d/007_geo_boundaries.sql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U threatwatch']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: threatwatch_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data: